class ExpensesItemModel {
  final String title, iconAsset, price, date;

  ExpensesItemModel({
    required this.title,
    required this.iconAsset,
    required this.price,
    required this.date,
  });
}
