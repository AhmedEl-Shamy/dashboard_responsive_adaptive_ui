class BankCardModel {
  final String cardName, cardNumber, securityCode, expirationDate;

  const BankCardModel({
    required this.cardName,
    required this.cardNumber,
    required this.securityCode,
    required this.expirationDate,
  });
}
